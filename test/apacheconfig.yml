---
- name: Conifg index
  hosts: all
  become: yes
  tasks:

   - name: write apache config 
     template: src=apacheconfig.j2 dest=/etc/httpd/conf/httpd.conf

   - name: start apache 
     service: name=httpd state=restarted


  handlers:
   - name: start apache
     service: name=httpd state=reloaded
